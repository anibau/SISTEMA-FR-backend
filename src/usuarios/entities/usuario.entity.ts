// Re-export de entidades y tipos para compatibilidad con módulos
export { User as Usuario } from '../../auth/entities/user.entity';
export { UserRole as Role } from '../../common/decorators/roles.decorator';

